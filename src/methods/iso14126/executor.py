from __future__ import annotations

from methods.core.method_executor import MethodExecutor


class ISO14126Executor(MethodExecutor):
    """Thin pilot wrapper; the CLI still uses the generic MethodExecutor."""

