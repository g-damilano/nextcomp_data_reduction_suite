{% for section in page.sections %}<section id="audit_rc_{{ section.section_id_html }}"><h2>{{ section.title_html }}</h2>{{ section.summary_html }}{{ section.evidence_detail_html }}</section>{% endfor %}
